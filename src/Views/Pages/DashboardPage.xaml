<Page x:Class="VibeVoice.Views.Pages.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:conv="clr-namespace:VibeVoice.Converters"
      mc:Ignorable="d"
      d:DesignHeight="580" d:DesignWidth="680"
      Title="主控台">

    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:BoolToVisibilityConverter x:Key="InvBoolToVis" Parameter="Invert"/>
        <conv:InverseBoolConverter x:Key="InvBool"/>
        <conv:AudioLevelToWidthConverter x:Key="AudioLevelToWidth"/>
        <conv:NullToVisibilityConverter x:Key="NullToVis"/>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32,24,32,24" MaxWidth="600" HorizontalAlignment="Center">

            <!-- Record Button -->
            <Border CornerRadius="80" Width="160" Height="160"
                    HorizontalAlignment="Center" Margin="0,0,0,16">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                <Setter Property="Background" Value="#C42B1C"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Button Command="{Binding ToggleRecordingCommand}"
                        IsEnabled="{Binding IsProcessing, Converter={StaticResource InvBool}}"
                        Background="Transparent" BorderThickness="0"
                        Width="160" Height="160" Cursor="Hand">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ui:SymbolIcon Symbol="Mic48" FontSize="48" Foreground="White"
                                       Visibility="{Binding IsRecording, Converter={StaticResource InvBoolToVis}}"/>
                        <ui:SymbolIcon Symbol="RecordStop48" FontSize="48" Foreground="White"
                                       Visibility="{Binding IsRecording, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>
                </Button>
            </Border>

            <!-- Status -->
            <TextBlock Text="{Binding StatusText}" FontSize="16" FontWeight="SemiBold"
                       HorizontalAlignment="Center" Margin="0,0,0,8"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

            <!-- Processing indicator -->
            <ProgressBar IsIndeterminate="True" Height="4" Margin="0,0,0,16"
                         Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVis}}"/>

            <!-- Audio Level -->
            <Border Background="{DynamicResource ControlFillColorDefaultBrush}"
                    CornerRadius="4" Height="12" Margin="0,0,0,24"
                    Visibility="{Binding IsRecording, Converter={StaticResource BoolToVis}}">
                <Border x:Name="AudioLevelBar" HorizontalAlignment="Left"
                        Background="{DynamicResource SystemAccentColorPrimaryBrush}"
                        CornerRadius="4" Height="12">
                    <Border.Width>
                        <MultiBinding Converter="{StaticResource AudioLevelToWidth}">
                            <Binding Path="AudioLevel"/>
                            <Binding RelativeSource="{RelativeSource AncestorType=Border, AncestorLevel=1}" Path="ActualWidth"/>
                        </MultiBinding>
                    </Border.Width>
                </Border>
            </Border>

            <!-- Preview Text -->
            <ui:Card Margin="0,0,0,24">
                <StackPanel>
                    <Grid Margin="0,0,0,8">
                        <TextBlock Text="辨識結果" FontSize="12" Opacity="0.6" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="複製" Padding="8,3" FontSize="12"
                                    Command="{Binding CopyPreviewCommand}"
                                    Background="Transparent" BorderThickness="0"
                                    Visibility="{Binding PreviewText, Converter={StaticResource NullToVis}}"/>
                            <Button Content="發送" Padding="8,3" FontSize="12" Margin="4,0,0,0"
                                    Command="{Binding SendPreviewCommand}"
                                    Background="Transparent" BorderThickness="0"
                                    Visibility="{Binding PreviewText, Converter={StaticResource NullToVis}}"/>
                        </StackPanel>
                    </Grid>
                    <TextBox Text="{Binding PreviewText, Mode=OneWay}"
                             IsReadOnly="True" TextWrapping="Wrap"
                             MinHeight="80" MaxHeight="160"
                             Background="Transparent" BorderThickness="0"
                             FontSize="15" VerticalScrollBarVisibility="Auto">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </StackPanel>
            </ui:Card>

            <!-- Mode Toggle Row -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Chinese Mode -->
                <ui:Card Grid.Column="0">
                    <StackPanel>
                        <TextBlock Text="中文模式" FontSize="12" Opacity="0.6" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <RadioButton Content="繁體" IsChecked="{Binding IsTraditional}"
                                         Margin="0,0,16,0" FontSize="14"/>
                            <RadioButton Content="簡體" IsChecked="{Binding IsSimplified}"
                                         FontSize="14"/>
                        </StackPanel>
                    </StackPanel>
                </ui:Card>

                <!-- Punctuation Mode -->
                <ui:Card Grid.Column="2">
                    <StackPanel>
                        <TextBlock Text="標點模式" FontSize="12" Opacity="0.6" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <RadioButton Content="無" IsChecked="{Binding PunctuationNone}"
                                         Margin="0,0,8,0" FontSize="13"/>
                            <RadioButton Content="中文" IsChecked="{Binding PunctuationChinese}"
                                         Margin="0,0,8,0" FontSize="13"/>
                            <RadioButton Content="英文" IsChecked="{Binding PunctuationEnglish}"
                                         FontSize="13"/>
                        </StackPanel>
                    </StackPanel>
                </ui:Card>
            </Grid>

            <!-- API Status -->
            <Grid HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Ellipse Grid.Column="0" Width="8" Height="8" VerticalAlignment="Center">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="#C42B1C"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsApiOnline}" Value="True">
                                    <Setter Property="Fill" Value="#0F7B0F"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Grid.Column="2" Text="{Binding ApiStatusText}"
                           FontSize="12" Opacity="0.7" VerticalAlignment="Center"/>
                <Button Grid.Column="4" Content="重新檢查"
                        Command="{Binding CheckApiStatusCommand}"
                        Padding="8,4" FontSize="12"/>
            </Grid>

        </StackPanel>
    </ScrollViewer>
</Page>
