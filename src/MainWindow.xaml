<ui:FluentWindow x:Class="VibeVoice.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="VibeVoice" Height="640" Width="900"
        MinHeight="560" MinWidth="720"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0" Grid.ColumnSpan="2"
                     Title="VibeVoice"
                     ShowMinimize="True"
                     ShowMaximize="False"
                     ShowClose="True"/>

        <!-- Sidebar -->
        <Border Grid.Row="1" Grid.Column="0"
                Background="{DynamicResource ApplicationBackgroundBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="0,0,1,0">
            <StackPanel Margin="8,16,8,16">
                <ui:NavigationViewItem x:Name="NavDashboard"
                    Content="主控台" Icon="{ui:SymbolIcon Symbol=Mic24}"
                    Margin="0,4" Cursor="Hand"
                    MouseLeftButtonDown="NavDashboard_Click"/>
                <ui:NavigationViewItem x:Name="NavHistory"
                    Content="歷史記錄" Icon="{ui:SymbolIcon Symbol=History24}"
                    Margin="0,4" Cursor="Hand"
                    MouseLeftButtonDown="NavHistory_Click"/>
                <ui:NavigationViewItem x:Name="NavSettings"
                    Content="設定" Icon="{ui:SymbolIcon Symbol=Settings24}"
                    Margin="0,4" Cursor="Hand"
                    MouseLeftButtonDown="NavSettings_Click"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Frame x:Name="ContentFrame" Grid.Row="1" Grid.Column="1"
               NavigationUIVisibility="Hidden"/>
    </Grid>
</ui:FluentWindow>

